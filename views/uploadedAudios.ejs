<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uploaded Audio</title>
</head>
<body>
    <h1>Uploaded Audio</h1>

    <% if (audio_url) { %>
        <p>Download your audio <a href="<%= audio_url %>" download>here</a>.</p>
        <audio controls>
            <source src="<%= audio_url %>" type="<%= audio_mime_type %>">
            Your browser does not support the audio element.
        </audio>
    <% } else { %>
        <p>No audio uploaded.</p>
    <% } %>
</body>
</html>
